#!/usr/bin/env node

const { InputError } = require('./lib/version/input-error')
const { version } = require('./lib/version/version')

version(process.argv.slice(2), process.stderr).catch(error => {
  if (error instanceof InputError) {
    console.error(error.message)
    process.exit(1)
  } else {
    console.error(error)
    process.exit(2)
  }
})
